
{% set location_path = path( location ) %}

<div class="content-view-line clearfix">

    <h2 class="att-title"><a href="{{ location_path }}">{{ ez_render_field( content, 'name' ) }}</a></h2>

    {% if not ez_is_field_empty( content, 'image' ) %}
        {{ ez_render_field(
            content,
            'image', {
                'parameters': {
                    'alias': 'small',
                    'link_href': location_path,
                    'link_class': 'att-image pull-left'
                }
            }
        ) }}
    {% endif %}

    {% if not ez_is_field_empty( content, 's' ) %}
        <div class="att-line-intro">
            {{ ez_render_field( content, 'line_intro' ) }}
        </div>
    {% elseif not ez_is_field_empty( content, 'full_intro' ) %}
        <div class="att-line-intro">
            {{ ez_render_field( content, 'full_intro' ) }}
        </div>
    {% endif %}

</div>
